# V2.12 — Member Dashboard

The private member landing page after login is now the NFL Degenerates Member Dashboard.

The previous NFL weekly results page remains available under `/results`.

Commissioner announcements are managed from Test Lab and use the existing `site_settings` table. No V2.12 schema migration is required.
