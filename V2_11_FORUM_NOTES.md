# V2.11 — Member Message Forum

The new `/forum` area is available only after member login.

Members can create topics and replies using a display name. Topic/reply text is HTML escaped in the browser before rendering.

Commissioner moderation is protected by the existing `ADMIN_PASSWORD`.

Run `v2_11_forum_schema_update.sql` before deployment.
